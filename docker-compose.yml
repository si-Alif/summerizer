services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: summerizer
      POSTGRES_PASSWORD: pa55word
      POSTGRES_DB: summerizer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
